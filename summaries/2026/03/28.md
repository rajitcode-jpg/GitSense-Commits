# Activity Summary for 3/28/2026

## 10:15:34 PM
The provided log details the development of a single Python file, `c:\Users\User\Documents\Python all\Python Practic\python_44.py`, entirely within the timeframe of March 28, 2026, specifically between 8:15 PM and 8:52 PM.

The development progresses through two distinct phases:

**Phase 1: `programer` Class Development (8:15 PM - 8:38 PM)**
The file starts empty at 8:15:45 PM.
*   **8:33 PM:** A `programer` class is introduced.
*   **8:34 PM:** A class attribute `company` is set to "Microsoft".
*   **8:34 PM - 8:35 PM:** The `__init__` method is progressively defined, taking `name`, `salary`, and `language` as parameters and assigning them to instance attributes.
*   **8:35 PM:** Three instances of `programer` (`p1`, `p2`, `p3`) are created with different data (e.g., Rohit, Sohail).
*   **8:36 PM:** A `display` method is added to the class, formatted to print the programer's name, salary, and language.
*   **8:36 PM - 8:37 PM:** The instances' `display()` method is called to output their information.

**Phase 2: `calculator` Class Development (8:48 PM - 8:52 PM)**
*   **8:48 PM:** The `programer` class and its related code are completely replaced by a new `calculator` class. This class is initialized with two numbers, `num1` and `num2`.
*   **8:49 PM - 8:50 PM:** Basic arithmetic methods (`add`, `sub`, `mul`) are implemented, followed by `div`.
*   **8:51 PM:** User input prompts are added to allow the user to enter `num1` and `num2` dynamically.
*   **8:51 PM:** An instance of the `calculator` class (`calc`) is created using the user's input, and all defined arithmetic methods (`add`, `sub`, `mul`, `div`) are called on this instance.
*   **8:52 PM:** A `pow` method for exponentiation (`num1**num2`) is added to the `calculator` class, and a call to `calc.pow()` is included.

**Key Patterns:**
*   The development shows an iterative approach, with small, incremental changes made in rapid succession to build up the functionality of each class.
*   There's a clear focus on demonstrating fundamental Object-Oriented Programming (OOP) concepts in Python, such as class definition, `__init__` methods, instance attributes, and instance methods.
*   The entire log represents a single session of coding practice or experimentation, as evidenced by the continuous timestamps on the same day, moving from one distinct programming example to another by overwriting the previous code.